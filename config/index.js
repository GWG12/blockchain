export const GENESIS_DATA = {
  blockHeaders: {
    parentHash: "start-hash",
    beneficiary: "start-beneficiary",
    difficulty: 1,
    number: 0,
    timestamp: "start-timestamp",
    nonce: 0,
  },
};

const MILLISECONDS = 1;
const SECONDS = 1000 * MILLISECONDS;
export const MINING_RATE = 13 * SECONDS;
